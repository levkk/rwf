use rwf::prelude::*;

// #[macros::controller]
// async fn test(request: &Request) -> Result<Response, Error> {
//     Ok(Response::new().text("hello"))
// }

fn main() {
    println!("Hello, world!");
}
